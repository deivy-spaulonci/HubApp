import { IbgeCidadePipe } from './ibge-cidade-pipe';

describe('IbgeCidadePipe', () => {
  it('create an instance', () => {
    const pipe = new IbgeCidadePipe();
    expect(pipe).toBeTruthy();
  });
});
